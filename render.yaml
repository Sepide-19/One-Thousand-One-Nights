services:
  - type: web
    name: One-Thousand-One-Nights-19
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 180
    envVars:
      - key: OPENAI_API_KEY
        sync: false
